enum UserRole { restaurateur, influencer, closer, commercial, admin }
enum SubscriptionType { free, medium, premium }
enum RestaurantStatus { pending, approved, rejected, suspended }
enum TransactionType { subscription, commission, boost, refund }
enum PaymentMethod { creditCard, stripe }
enum TransactionStatus { pending, completed, failed }
